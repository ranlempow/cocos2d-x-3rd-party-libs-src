diff -Eaur a1/CMakeLists.txt b1/CMakeLists.txt
--- a1/CMakeLists.txt	2017-10-26 16:05:25.302374700 +0800
+++ b1/CMakeLists.txt	2017-10-27 00:10:03.929996200 +0800
@@ -387,7 +387,14 @@
 
 
 IF(NOT DYNLOAD)
-    SET(EXTRA_LIBS ${SNDFILE_LIBRARIES} ${VORBISFILE_LIBRARIES} ${LIBFLAC_LIBRARIES} ${MPG123_LIBRARIES} ${DUMB_LIBRARIES} ${MODPLUG_LIBRARIES} ${FLUIDSYNTH_LIBRARIES} ${EXTRA_LIBS})
+    foreach(lib SNDFILE VORBISFILE FLAC MPG123 DUMB MODPLUG FLUIDSYNTH)
+        IF(${lib}_LIBRARIES)
+            SET(EXTRA_LIBS ${${lib}_LIBRARIES} ${EXTRA_LIBS})
+        ENDIF()
+        IF(${lib}_LIBRARY)
+            SET(EXTRA_LIBS ${${lib}_LIBRARY} ${EXTRA_LIBS})
+        ENDIF()
+    endforeach(lib)
 ELSE(NOT DYNLOAD)
     ADD_DEFINITIONS(-DDYNLOAD=1)
 ENDIF(NOT DYNLOAD)
